---
recType: code
group: pandas
recID: py.pd.df.001
keywords: [dataframe, iterator, columns, items]
textkey:
  - "iterate over columns of a dataframe"
code: "for $i$ in $dataframe$.iteritems():\n    $END$"
parameter:
  - name: dataframe
    expr: "pyIterableVariable"
  - name: i
    expr: "collectionElementName"
commentTemplate: "Iterator over (column name, Series) pairs"
---
recType: code
group: pandas
recID: py.pd.df.002
keywords: [dataframe, iterator, rows]
textkey:
  - "iterate over rows of a dataframe"
code: "for $i$ in $dataframe$.iterrows():\n    $END$"
parameter:
  - name: dataframe
    expr: "pyIterableVariable"
  - name: i
    expr: "collectionElementName"
commentTemplate: "Iterate over (rowindex, Series) pairs"
---
recType: code
group: pandas
recID: py.pd.df.003
keywords: [dataframe, iterator, rows, tuples, namedtuples]
textkey:
  - "iterate over rows as namedtuples"
code: "for $i$ in $dataframe$.itertuples(index=$index$, name=\"$name$\"):\n    $END$"
parameter:
  - name: i
    expr: "collectionElementName"
  - name: dataframe
    expr: "pyIterableVariable"
  - name: index
    vars: True|False
  - name: name
    vars: Pandas
commentTemplate: "Iterate over DataFrame rows as namedtuples, with index value as first element of the tuple"
---
recType: code
group: pandas
recID: py.pd.df.004
keywords: [dataframe, dot, product, matrix, multiplication]
textkey:
  - "dot product with other dataframe|series object"
  - "matrix multiplication with dataframe|Series objects"
code: |
    $dataframe$.dot($dataframe$)
parameter:
  - name: dataframe
    expr: "pyIterableVariable"
commentTemplate: "Matrix multiplication with DataFrame or Series objects"
---
recType: code
group: pandas
recID: py.pd.df.005
keywords: [dataframe, transpose, matrix, diagonal]
textkey:
  - "transpose index and columns of a dataframe"
  - "flip|reflect a matrix over its diagonal"
code: |
    $dataframe$.transpose()
parameter:
  - name: dataframe
    expr: "pyIterableVariable"
commentTemplate: "Transpose index and columns"